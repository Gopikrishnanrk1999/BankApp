import { DashbgDirective } from './dashbg.directive';

describe('DashbgDirective', () => {
  it('should create an instance', () => {
    const directive = new DashbgDirective();
    expect(directive).toBeTruthy();
  });
});
